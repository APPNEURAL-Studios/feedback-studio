{
  "name": "@appneural/feedback-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Feedback Studio — served at feedback.studio.appneural.com",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=20"
  }
}
